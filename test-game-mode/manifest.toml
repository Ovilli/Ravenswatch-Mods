[mod]
id           = "test-game-mode"
name         = "TEST: repeat chapter order"
version      = "0.1.0"
author       = "RSMM"
description  = "Dev test. The run plays Storm Island FIRST, then Dark Hills. Pass: chapter 2 is Dark Hills (rsmm log --grep tilegen-proof names it); a jump to Avalon or the run ending means only ascending orders work."
enabled      = false
load_order   = 96
sdk_version  = ">=3.0,<4"
license      = "MIT"
tags         = ["dev", "testing"]
multiplayer_scope = "deterministic-shared"
experimental = true

[[content]]
kind     = "game_mode"
id       = "All_Chapters"
base     = "All_Chapters"
chapters = [1, 0]
