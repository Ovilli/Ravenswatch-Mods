Parked 2026-09-06. It existed only because scenery cannot carry a marker or
an interaction, so the icon and prompt needed a second entity beside the
model. That separation was measured in-game as the 'offset' -- 0.98 units
between the obelisk and the thing you could actually use. `BonFire` is
non-scenery and carries both, so one object does the whole job.
