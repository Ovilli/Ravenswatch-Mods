[mod]
id          = "hero-probe"
name        = "DEV: hero roster probe"
version     = "0.1.0"
author      = "RSMM"
description = "Dev probe, delete after use. Names the registry class the hero-select screen enumerates (key stored at 0x1414762c8) and counts live hero defs. Read-only."
enabled     = true
load_order  = 97
sdk_version = ">=3.0,<4"
license     = "MIT"
tags        = ["dev", "testing"]
multiplayer_scope = "local-only"
