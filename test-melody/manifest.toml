[mod]
id           = "test-melody"
name         = "TEST: melody retune"
version      = "0.1.0"
author       = "RSMM"
description  = "Dev test. Makes the Piper's Fully Heal melody reveal the map instead. Pass: as the Piper, collect and play Fully Heal and the map is revealed."
enabled     = true
load_order   = 96
sdk_version  = ">=3.0,<4"
license      = "MIT"
tags         = ["dev", "testing"]
multiplayer_scope = "deterministic-shared"
experimental = true

[[content]]
kind   = "melody"
id     = "test_fully_heal"
base   = "Fully_Heal"
effect = "Reveal_Map"
