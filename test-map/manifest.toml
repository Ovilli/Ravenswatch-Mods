[mod]
id           = "test-map"
name         = "TEST: cloned map in chapter 1"
version      = "0.1.0"
author       = "RSMM"
description  = "Dev test. Clones Dark Hills (on its own root + tile-generation levels) and makes the first chapter play the clone. Enable with runestone-shrine, which adds its tile to the VANILLA Dark Hills pool only. Pass: chapter 1 loads and `rsmm log --grep shrine-probe` says NONE ours (the clone generates from its own pool). Before 2026-09-24 it placed the shrine 6 times."
enabled      = false
load_order   = 96
sdk_version  = ">=3.0,<4"
license      = "MIT"
tags         = ["dev", "testing"]
multiplayer_scope = "deterministic-shared"
experimental = true

[[content]]
kind    = "map"
id      = "Test_Knight_Hills"
base    = "Dark_Hills"
tribe   = "Knights"
chapter = 0
